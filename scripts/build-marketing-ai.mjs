import fs from 'node:fs';
import path from 'node:path';

const root = process.cwd();
const read = p => JSON.parse(fs.readFileSync(path.join(root, p), 'utf8'));
const esc = (s='') => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const store = read('content/store.json');
const menus = read('content/menus.json');
const config = read('content/marketing-ai-cases.json');
const out = path.join(root, 'dist', 'marketing-ai');
fs.mkdirSync(out, {recursive:true});

const css = `
:root{--bg:#f4f6f8;--panel:#fff;--ink:#17202a;--muted:#66727e;--line:#dfe5ea;--accent:#17202a;--soft:#eef2f5}*{box-sizing:border-box}[hidden]{display:none!important}body{margin:0;background:var(--bg);color:var(--ink);font-family:system-ui,-apple-system,"Noto Sans KR",sans-serif}a{text-decoration:none;color:inherit}button,input,select{font:inherit}.shell{min-height:100vh;display:flex;flex-direction:column}.top{padding:20px clamp(18px,4vw,48px);display:flex;align-items:center;justify-content:space-between;gap:20px}.top-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:flex-end}.product{font-size:18px;font-weight:900;letter-spacing:-.03em}.badge{font-size:12px;font-weight:800;padding:7px 10px;border-radius:999px;background:var(--soft);color:var(--muted)}main{width:min(1080px,100%);margin:auto;padding:32px 20px 64px}.hero{text-align:center;max-width:760px;margin:4vh auto 34px}.eyebrow{font-size:12px;font-weight:900;letter-spacing:.12em;color:var(--muted)}h1{font-size:clamp(36px,6vw,68px);line-height:1.04;letter-spacing:-.055em;margin:12px 0}.hero p{font-size:clamp(16px,2vw,20px);line-height:1.65;color:var(--muted);margin:0}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.card{background:var(--panel);border:1px solid var(--line);border-radius:20px;padding:20px;min-height:154px}.card small{display:block;color:var(--muted);margin-bottom:10px}.card strong{font-size:20px;letter-spacing:-.03em}.card p{color:var(--muted);font-size:14px;line-height:1.55;margin:10px 0 0}.menu{margin-top:14px;background:var(--panel);border:1px solid var(--line);border-radius:20px;padding:20px}.menu h2{margin:0 0 12px;font-size:18px}.row{display:flex;justify-content:space-between;gap:20px;padding:12px 0;border-top:1px solid var(--soft)}.row:first-of-type{border-top:0}.row b{white-space:nowrap}.empty{color:var(--muted);padding:12px 0}.actions{display:flex;justify-content:center;gap:10px;flex-wrap:wrap;margin-top:28px}.btn,.auth-open{border:0;border-radius:14px;padding:13px 18px;font-weight:850;background:var(--accent);color:#fff;cursor:pointer}.btn[disabled]{opacity:.42;cursor:not-allowed}.auth-open{padding:9px 12px;border-radius:11px;font-size:12px}.powered{margin-top:auto;text-align:center;padding:20px;font-size:12px;font-weight:800;color:#4e5964}.powered span{font-weight:950;color:#111820}.case-list{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.case-card{background:#fff;border:1px solid var(--line);border-radius:18px;padding:20px}.case-card.disabled{opacity:.6}.case-card strong{display:block;font-size:20px;margin:8px 0}.case-card small{color:var(--muted)}.qa-card{display:flex;flex-direction:column;gap:12px}.qa-status{display:inline-flex;align-self:flex-start;font-size:12px;font-weight:900;padding:6px 9px;border-radius:999px;background:#fff4d6;color:#7a4d00}.qa-open{display:inline-flex;align-items:center;justify-content:center;margin-top:auto;background:var(--accent);color:#fff;padding:12px 14px;border-radius:12px;font-weight:900}.qa-note{margin-top:20px;text-align:center;color:var(--muted);font-size:13px;line-height:1.6}.auth-panel{position:fixed;inset:0;z-index:80;background:rgba(17,24,39,.62);backdrop-filter:blur(8px);display:grid;place-items:center;padding:18px}.auth-dialog{width:min(100%,480px);max-height:calc(100vh - 36px);overflow:auto;background:#fff;border-radius:24px;border:1px solid var(--line);box-shadow:0 30px 90px rgba(0,0,0,.28);padding:24px}.auth-dialog-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start}.auth-dialog h2{margin:5px 0 8px;font-size:27px;letter-spacing:-.04em}.auth-copy{color:var(--muted);font-size:13px;line-height:1.6;margin:0 0 18px}.auth-close{border:1px solid var(--line);background:#fff;border-radius:10px;width:36px;height:36px;cursor:pointer}.auth-form{display:grid;gap:12px}.auth-form label{display:grid;gap:6px;color:#4e5964;font-size:12px;font-weight:750}.auth-form input,.auth-form select{width:100%;padding:12px 13px;border:1px solid #cfd7df;border-radius:12px;background:#fff;color:#17202a;outline:none}.auth-form input:focus,.auth-form select:focus{border-color:#6b7280}.auth-submit{border:0;background:#17202a;color:#fff;border-radius:12px;padding:13px;font-weight:900;cursor:pointer}.auth-submit:disabled{opacity:.55}.auth-error{min-height:18px;color:#b42318;font-size:12px;margin:0}.auth-account{display:grid;gap:13px}.auth-account-card{border:1px solid var(--line);border-radius:16px;padding:16px;background:#f8fafb}.auth-account-card small{display:block;color:var(--muted);margin-bottom:5px}.auth-account-card strong{display:block;font-size:18px}.auth-account-meta{display:grid;grid-template-columns:1fr 1fr;gap:10px}.auth-account-meta div{padding:12px;border:1px solid var(--line);border-radius:12px}.auth-account-meta small{display:block;color:var(--muted);font-size:11px;margin-bottom:4px}.auth-logout{border:1px solid #cfd7df;background:#fff;border-radius:12px;padding:12px;font-weight:800;cursor:pointer}.auth-security{font-size:11px;line-height:1.55;color:var(--muted);padding-top:5px}.auth-invite-badge{display:inline-flex;padding:6px 9px;border-radius:999px;background:#e9f7ef;color:#146c43;font-size:11px;font-weight:900;margin-bottom:8px}@media(max-width:760px){.top{padding:16px 18px;align-items:flex-start}.grid,.case-list{grid-template-columns:1fr}.hero{margin-top:2vh}.card{min-height:0}main{padding-top:18px}h1{font-size:44px}.top-actions{max-width:55%}.auth-account-meta{grid-template-columns:1fr}}
`;

const brandById = new Map((menus.brands || []).map(b => [b.id, b]));
const customDomains = Object.freeze({
  jadam: 'https://jadam.ekodi.kr/',
  pizzamaru: 'https://pizzamaru.ekodi.kr/',
  yogurtpurple: 'https://yogurt.ekodi.kr/'
});
const tenantByCaseSlug = Object.freeze({ jadam: 'jadam', pizzamaru: 'pizzamaru', yogurtpurple: 'yogurt' });

function customerAuthMarkup() {
  return `<section class="auth-panel" id="authPanel" hidden aria-modal="true" role="dialog" aria-labelledby="authTitle"><div class="auth-dialog"><div class="auth-dialog-head"><div><span class="auth-invite-badge" id="authModeBadge">고객 전용</span><h2 id="authTitle">고객 로그인</h2></div><button class="auth-close" id="authClose" type="button" aria-label="닫기">×</button></div><p class="auth-copy" id="authCopy">EKODI가 등록한 고객 계정으로만 관리공간에 들어갈 수 있습니다.</p><form class="auth-form" id="customerLoginForm"><label>이메일<input name="email" type="email" autocomplete="username" required></label><label>비밀번호<input name="password" type="password" minlength="12" autocomplete="current-password" required></label><p class="auth-error" id="loginError" role="alert"></p><button class="auth-submit" type="submit">고객 관리공간 로그인</button></form><form class="auth-form" id="customerInviteForm" hidden><label>이름<input name="displayName" type="text" maxlength="100" autocomplete="name" required></label><label>비밀번호 만들기<input name="password" type="password" minlength="12" autocomplete="new-password" required placeholder="12자 이상"></label><label>비밀번호 확인<input name="confirmPassword" type="password" minlength="12" autocomplete="new-password" required></label><p class="auth-error" id="inviteError" role="alert"></p><button class="auth-submit" type="submit">초대 수락하고 계정 활성화</button></form><div class="auth-account" id="customerAccount" hidden><div class="auth-account-card"><small>인증된 고객공간</small><strong id="accountTenant"></strong><span id="accountEmail"></span></div><div class="auth-account-meta"><div><small>권한</small><strong id="accountRole"></strong></div><div><small>세션 만료</small><strong id="accountExpiry"></strong></div></div><p class="auth-security">이 로그인은 현재 고객 사이트에서만 사용됩니다. 다른 고객사의 데이터와 권한은 서버에서 분리됩니다.</p><button class="auth-logout" id="customerLogout" type="button">로그아웃</button></div></div></section>`;
}

function customerAuthScript(tenantSlug) {
  const tenant = JSON.stringify(tenantSlug);
  return `<script>(()=>{const API='https://api.ekodi.kr';const TENANT=${tenant};const KEY='ekodi-customer-token';const roles={client_admin:'고객 관리자',client_editor:'콘텐츠 편집자',client_viewer:'조회·검수자'};const q=s=>document.querySelector(s);const panel=q('#authPanel'),open=q('#customerAuthOpen'),close=q('#authClose'),login=q('#customerLoginForm'),invite=q('#customerInviteForm'),account=q('#customerAccount'),title=q('#authTitle'),copy=q('#authCopy'),badge=q('#authModeBadge'),loginError=q('#loginError'),inviteError=q('#inviteError');const token=()=>sessionStorage.getItem(KEY)||'';async function api(path,options={}){const headers=new Headers(options.headers||{});if(token())headers.set('authorization','Bearer '+token());if(options.body&&!headers.has('content-type'))headers.set('content-type','application/json');const response=await fetch(API+path,{...options,headers,cache:'no-store'});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.error||('인증 요청 실패 ('+response.status+')'));return data}function show(){panel.hidden=false;document.body.style.overflow='hidden'}function hide(){panel.hidden=true;document.body.style.overflow=''}function modeLogin(){badge.textContent='고객 전용';title.textContent='고객 로그인';copy.textContent='EKODI가 등록한 고객 계정으로만 관리공간에 들어갈 수 있습니다.';login.hidden=false;invite.hidden=true;account.hidden=true}function modeInvite(){badge.textContent='초대 확인';title.textContent='고객 계정 활성화';copy.textContent='EKODI에서 받은 1회용 초대를 확인했습니다. 본인 이름과 새 비밀번호를 등록해 주세요.';login.hidden=true;invite.hidden=false;account.hidden=true}function format(value){const d=new Date(value);return Number.isNaN(d.getTime())?'확인 필요':d.toLocaleString('ko-KR')}function showAccount(data){badge.textContent='인증 완료';title.textContent='고객 관리공간';copy.textContent='현재 고객 테넌트의 계정과 권한이 확인되었습니다.';login.hidden=true;invite.hidden=true;account.hidden=false;q('#accountTenant').textContent=data.tenant?.name||TENANT;q('#accountEmail').textContent=data.email||'';q('#accountRole').textContent=roles[data.role]||data.role||'고객 사용자';q('#accountExpiry').textContent=format(data.expiresAt);open.textContent='고객 관리 ✓'}async function restore(){if(!token())return;try{showAccount(await api('/api/customer/session'))}catch{sessionStorage.removeItem(KEY);modeLogin()}}open.addEventListener('click',async()=>{show();if(token())await restore();else modeLogin()});close.addEventListener('click',hide);panel.addEventListener('click',e=>{if(e.target===panel)hide()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!panel.hidden)hide()});login.addEventListener('submit',async e=>{e.preventDefault();loginError.textContent='';if(!login.checkValidity())return login.reportValidity();const b=login.querySelector('button');b.disabled=true;b.textContent='인증 중…';try{const form=new FormData(login);const data=await api('/api/customer/login',{method:'POST',body:JSON.stringify({tenant:TENANT,email:String(form.get('email')).trim().toLowerCase(),password:String(form.get('password'))})});sessionStorage.setItem(KEY,data.token);login.reset();showAccount(data)}catch(error){loginError.textContent=error.message}finally{b.disabled=false;b.textContent='고객 관리공간 로그인'}});invite.addEventListener('submit',async e=>{e.preventDefault();inviteError.textContent='';if(!invite.checkValidity())return invite.reportValidity();const form=new FormData(invite),password=String(form.get('password')),confirm=String(form.get('confirmPassword'));if(password!==confirm){inviteError.textContent='비밀번호가 일치하지 않습니다.';return}const params=new URLSearchParams(location.search),raw=params.get('ekodi_invite')||'';const b=invite.querySelector('button');b.disabled=true;b.textContent='계정 활성화 중…';try{const data=await api('/api/customer/accept-invite',{method:'POST',body:JSON.stringify({token:raw,displayName:String(form.get('displayName')).trim(),password})});sessionStorage.setItem(KEY,data.token);params.delete('ekodi_invite');const next=location.pathname+(params.toString()?'?'+params.toString():'')+location.hash;history.replaceState(null,'',next);invite.reset();showAccount(data)}catch(error){inviteError.textContent=error.message}finally{b.disabled=false;b.textContent='초대 수락하고 계정 활성화'}});q('#customerLogout').addEventListener('click',async()=>{try{if(token())await api('/api/customer/logout',{method:'POST'})}catch{}sessionStorage.removeItem(KEY);open.textContent='고객 로그인';modeLogin()});const inviteToken=new URLSearchParams(location.search).get('ekodi_invite');if(inviteToken){modeInvite();show()}else restore()})();</script>`;
}

for (const item of config.cases || []) {
  const brand = brandById.get(item.brandId);
  const menuRows = (brand?.items || []).filter(m => Number.isInteger(m.price) && m.price >= 0).map(m => `<div class="row"><span>${esc(m.name)}</span><b>${Number(m.price).toLocaleString('ko-KR')}원</b></div>`).join('');
  const menuBody = menuRows || `<div class="empty">가격이 확인된 메뉴부터 순차적으로 연결합니다. 확인되지 않은 가격은 표시하지 않습니다.</div>`;
  const tenantSlug = tenantByCaseSlug[item.slug];
  if (!tenantSlug) throw new Error(`Marketing AI tenant mapping missing: ${item.slug}`);
  const dir = path.join(out, item.slug);
  fs.mkdirSync(dir, {recursive:true});
  const csp = `default-src 'self'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src https://api.ekodi.kr; img-src 'self' data:; object-src 'none'; base-uri 'self'; form-action 'self'`;
  const html = `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="robots" content="noindex,nofollow"><meta name="referrer" content="no-referrer"><meta http-equiv="Content-Security-Policy" content="${esc(csp)}"><title>${esc(item.storeName)} | ${esc(config.productName)}</title><meta name="description" content="${esc(item.storeName)} ${esc(config.productName)} 적용사례"><style>${css}</style></head><body><div class="shell"><header class="top"><div class="product">${esc(config.productName)}</div><div class="top-actions"><span class="badge">검수 중</span><button class="auth-open" id="customerAuthOpen" type="button">고객 로그인</button></div></header><main><section class="hero"><div class="eyebrow">MARKETING AI · ${esc(item.categoryLabel)}</div><h1>${esc(item.storeName)}</h1><p>${esc(item.headline)}. 매장 데이터와 콘텐츠, 채널 운영을 하나의 흐름으로 연결합니다.</p></section><section class="grid"><article class="card"><small>오늘의 콘텐츠</small><strong>추천 초안 준비</strong><p>매장 정보와 확인된 메뉴를 기준으로 게시 전 검토 가능한 콘텐츠를 준비합니다.</p></article><article class="card"><small>채널 운영</small><strong>한 곳에서 관리</strong><p>향후 네이버·인스타그램·유튜브 등 연결 채널을 점포별로 관리합니다.</p></article><article class="card"><small>매장 데이터</small><strong>${esc(store.phone)}</strong><p>${esc(store.address)}<br>${esc(store.hours.display)} · ${esc(store.hours.note)}</p></article></section><section class="menu"><h2>확인된 메뉴 데이터</h2>${menuBody}<p class="empty">기준 확인일 ${esc(menus.lastVerified)} · 주문 기능은 검수 완료 후에만 활성화됩니다.</p></section><div class="actions"><button class="btn" type="button" disabled>콘텐츠 만들기 · 검수 후 활성화</button><button class="btn" type="button" disabled>채널 게시 · 검수 후 활성화</button></div></main><footer class="powered">Powered by <span>${esc(config.poweredBy)}</span></footer></div>${customerAuthMarkup()}${customerAuthScript(tenantSlug)}</body></html>`;
  fs.writeFileSync(path.join(dir, 'index.html'), html);
}

const cards = (config.cases || []).map(item => config.publicLinksEnabled && item.status === 'approved'
  ? `<a class="case-card" href="./${esc(item.slug)}/"><small>${esc(item.categoryLabel)}</small><strong>${esc(item.storeName)}</strong><span>열기 →</span></a>`
  : `<article class="case-card disabled" aria-disabled="true"><small>${esc(item.categoryLabel)} · 검수 중</small><strong>${esc(item.storeName)}</strong><span>배포·검수 완료 후 공개</span></article>`).join('');
const index = `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>${esc(config.productName)} 적용사례</title><style>${css}</style></head><body><div class="shell"><header class="top"><div class="product">${esc(config.productName)}</div><span class="badge">APPLICATION CASES</span></header><main><section class="hero"><div class="eyebrow">MARKETING AI</div><h1>매장마다 같은 엔진,<br>각자의 브랜드.</h1><p>검수 완료된 점포만 공개 링크가 활성화됩니다.</p></section><section class="case-list">${cards}</section></main><footer class="powered">Powered by <span>${esc(config.poweredBy)}</span></footer></div></body></html>`;
fs.writeFileSync(path.join(out, 'index.html'), index);

const qaCards = (config.cases || []).map(item => {
  const href = customDomains[item.slug] || `../${esc(item.slug)}/`;
  const label = item.status === 'approved' ? '승인' : item.status === 'needs_changes' ? '수정 필요' : '승인 대기';
  return `<article class="case-card qa-card"><small>${esc(item.categoryLabel)}</small><strong>${esc(item.storeName)}</strong><span class="qa-status">${esc(label)}</span><a class="qa-open" href="${esc(href)}" target="_blank" rel="noopener">새창에서 검수하기 ↗</a></article>`;
}).join('');
const qaDir = path.join(out, 'qa');
fs.mkdirSync(qaDir, {recursive:true});
const qa = `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow,noarchive"><title>${esc(config.productName)} 검수센터</title><style>${css}</style></head><body><div class="shell"><header class="top"><div class="product">${esc(config.productName)} 검수센터</div><span class="badge">QA ONLY</span></header><main><section class="hero"><div class="eyebrow">MARKETING AI QA</div><h1>배포 전에<br>직접 확인합니다.</h1><p>각 점포를 새창으로 열어 브랜드명, 매장정보, 확인된 메뉴, 모바일 화면과 링크 동작을 검수합니다.</p></section><section class="case-list">${qaCards}</section><p class="qa-note">이 페이지는 검색엔진에 노출하지 않습니다. 공개 승인 전까지 마케팅AI 메인 화면의 점포 링크는 비활성 상태를 유지합니다.</p></main><footer class="powered">Powered by <span>${esc(config.poweredBy)}</span></footer></div></body></html>`;
fs.writeFileSync(path.join(qaDir, 'index.html'), qa);

console.log(`✅ Marketing AI 적용사례 ${config.cases?.length || 0}개 생성 완료`);
