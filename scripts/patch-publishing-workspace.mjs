import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const hub=path.join(root,'dist','marketing-ai','index.html');
const API='https://marketing-publish-api.ekodi.kr';
if(!fs.existsSync(hub))throw new Error('Marketing AI hub must exist before publishing workspace patch');
let html=fs.readFileSync(hub,'utf8');

html=html.replace(/<style data-publishing-workspace>[\s\S]*?<\/style>/g,'');
html=html.replace(/<script data-publishing-workspace>[\s\S]*?<\/script>/g,'');
html=html.replace(/<section class="publication-ops"[\s\S]*?<\/section><!-- publication-ops:end -->/g,'');

const panel=`<section class="publication-ops" aria-label="콘텐츠 발행과 예약"><div class="publication-head"><div><small>PUBLISHING & SCHEDULE</small><h3>만들고, 실제 채널에 뿌립니다</h3><p>콘텐츠 생성은 출발점입니다. 연결된 채널에 지금 발행하거나 날짜와 시간을 정해 예약하고, 성공·실패·재시도를 한곳에서 확인합니다.</p></div><span class="publish-status" id="publishEngineStatus">로그인 필요</span></div><div class="publication-grid"><article class="publish-card"><div class="publish-card-head"><div><small>BRAND SUBJECT</small><strong id="publishSubjectLabel">개인 또는 Workspace</strong></div><span id="publishSubjectType">PERSON</span></div><form id="brandProfileForm" class="publish-form"><label>브랜드 이름<input id="brandName" maxlength="120" placeholder="예: 홍길동 / 에코디교회"></label><label>한 줄 소개<input id="brandTagline" maxlength="240" placeholder="내가 누구이며 무엇을 전하는지"></label><label>주요 대상<input id="brandAudience" maxlength="500" placeholder="누구에게 알리고 싶은지"></label><button class="publish-secondary" type="submit">브랜드 저장</button></form><p class="publish-message" id="brandMessage"></p></article><article class="publish-card"><div class="publish-card-head"><div><small>CHANNELS</small><strong>실제 발행 채널</strong></div><button class="publish-link" id="refreshPublishChannels" type="button">새로고침</button></div><div id="publishChannelList" class="publish-channel-list"><p class="publish-empty">로그인하면 연결된 채널을 확인합니다.</p></div><p class="publish-note">채널 인증정보가 확인된 경우에만 자동 게시가 실행됩니다. 인증이 끊기면 발행하지 않고 <b>credentials_required</b> 상태로 멈춥니다.</p></article></div><article class="publish-card publish-compose"><div class="publish-card-head"><div><small>QUEUE</small><strong>지금 발행 · 예약 발행</strong></div><span id="publishPlanGate">PLAN 확인 중</span></div><div class="publish-queue-form"><label>발행 시간<input id="publishAt" type="datetime-local"></label><label>반복<select id="publishRecurrence"><option value="">반복 안 함</option><option value="daily">매일</option><option value="weekly">매주</option><option value="monthly">매월</option></select></label><div class="publish-actions"><button id="publishNow" type="button">지금 발행</button><button id="publishSchedule" type="button">예약 발행</button></div></div><p class="publish-message" id="publishMessage">먼저 위 콘텐츠 실습실에서 게시할 문구를 만든 뒤 채널을 선택하세요.</p></article><article class="publish-card"><div class="publish-card-head"><div><small>PUBLICATION LOG</small><strong>발행 상태</strong></div><button class="publish-link" id="refreshPublishJobs" type="button">새로고침</button></div><div id="publishJobList" class="publish-job-list"><p class="publish-empty">아직 발행 작업이 없습니다.</p></div></article></section><!-- publication-ops:end -->`;
if(!html.includes('<!-- member-workspace:end -->'))throw new Error('Member workspace marker missing before publishing patch');
html=html.replace('<!-- member-workspace:end -->',`<!-- member-workspace:end -->${panel}`);

const style=`<style data-publishing-workspace>
.publication-ops{margin:14px 0 18px;border:1px solid var(--line);border-radius:18px;background:linear-gradient(180deg,#fff,#f8faf9);padding:16px}.publication-head,.publish-card-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.publication-head small,.publish-card small{display:block;color:var(--muted);font-size:9px;font-weight:950;letter-spacing:.08em}.publication-head h3{margin:4px 0 3px;font-size:20px;letter-spacing:-.035em}.publication-head p{margin:0;max-width:720px;color:var(--muted);font-size:11px;line-height:1.5}.publish-status,#publishSubjectType,#publishPlanGate{border-radius:999px;background:var(--bg);border:1px solid var(--line);padding:6px 9px;font-size:9px;font-weight:900;white-space:nowrap}.publish-status.ready{background:#e9f7ef;color:#146c43}.publish-status.warn{background:#fff4d6;color:#7a4d00}.publication-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:13px}.publish-card{border:1px solid var(--line);border-radius:14px;background:#fff;padding:13px}.publish-card strong{font-size:12px}.publish-form{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}.publish-form label,.publish-queue-form label{display:grid;gap:5px;color:var(--muted);font-size:9px;font-weight:800}.publish-form label:nth-child(3){grid-column:1/-1}.publish-form input,.publish-queue-form input,.publish-queue-form select{width:100%;border:1px solid var(--line);background:#fff;border-radius:10px;padding:9px 10px;color:var(--ink);font-size:10px}.publish-form input:focus,.publish-queue-form input:focus,.publish-queue-form select:focus{outline:2px solid rgba(33,61,45,.14);border-color:#789181}.publish-secondary,.publish-actions button{border:0;border-radius:10px;background:var(--ink);color:#fff;padding:9px 11px;font-size:10px;font-weight:900;cursor:pointer}.publish-secondary{align-self:end}.publish-secondary:disabled,.publish-actions button:disabled{opacity:.42;cursor:not-allowed}.publish-link{border:0;background:transparent;padding:3px;color:#31523d;font-size:9px;font-weight:900;cursor:pointer}.publish-message,.publish-note,.publish-empty{margin:8px 0 0;color:var(--muted);font-size:9px;line-height:1.45}.publish-message.ok{color:#146c43;font-weight:800}.publish-message.error{color:#b42318;font-weight:800}.publish-channel-list{display:grid;gap:7px;margin-top:10px}.publish-channel{display:flex;align-items:center;gap:8px;border:1px solid var(--line);border-radius:10px;padding:8px 9px;background:var(--bg)}.publish-channel input{accent-color:#213d2d}.publish-channel span{margin-left:auto;font-size:8px;color:var(--muted)}.publish-channel.disabled{opacity:.58}.publish-compose{margin-top:10px}.publish-queue-form{display:grid;grid-template-columns:1fr .75fr auto;gap:8px;align-items:end;margin-top:10px}.publish-actions{display:flex;gap:6px}.publish-job-list{display:grid;gap:7px;margin-top:10px}.publish-job{display:grid;grid-template-columns:1.4fr .75fr .75fr;gap:8px;align-items:center;border:1px solid var(--line);border-radius:10px;padding:9px;background:var(--bg);font-size:9px}.publish-job strong{font-size:10px}.publish-job span{color:var(--muted)}.publish-job b{text-align:right}.job-published{color:#146c43}.job-failed,.job-credentials_required{color:#b42318}.job-retrying{color:#9a6700}@media(max-width:820px){.publication-grid,.publish-queue-form{grid-template-columns:1fr}.publish-actions{width:100%}.publish-actions button{flex:1}.publish-job{grid-template-columns:1fr}.publish-job b{text-align:left}}@media(max-width:540px){.publication-head{display:grid}.publish-form{grid-template-columns:1fr}.publish-form label:nth-child(3){grid-column:auto}}
</style>`;
html=html.replace('</head>',`${style}</head>`);

const runtime=`<script data-publishing-workspace>(()=>{
const API=${JSON.stringify(API)};const q=s=>document.querySelector(s);const selected=new Set();
const token=()=>window.EKODI_MARKETING_AUTH_TOKEN||'';const context=()=>window.EKODI_MARKETING_CONTEXT||null;const personalMode=()=>new URLSearchParams(location.search).get('mode')==='personal-brand';
function subject(){const c=context()||{};if(personalMode()||c.workspaceKind==='personal'||(!c.storeId&&!c.tenant&&!c.tenantId))return{type:'person',key:''};if(c.storeId)return{type:'store',key:c.storeId};return{type:'tenant',key:c.tenant||c.tenantId||''}}
function queryString(){const s=subject();const p=new URLSearchParams({subject_type:s.type});if(s.key)p.set('subject_key',s.key);return p.toString()}
async function api(path,options={}){if(!token())throw new Error('Google 로그인 후 사용할 수 있습니다.');const headers=new Headers(options.headers||{});headers.set('authorization','Bearer '+token());if(options.body&&!headers.has('content-type'))headers.set('content-type','application/json');const join=path.includes('?')?'&':'?';const r=await fetch(API+path+join+queryString(),{...options,headers,cache:'no-store'});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||('발행 API 오류 ('+r.status+')'));return d}
function plan(){const c=context()||{};return String(c.plan||'free').toLowerCase()}
function allowed(kind){const p=plan();const rank={free:0,flex:1,plus:2,pro:3,auto:4,enterprise:4};const n=rank[p]??0;if(kind==='now')return n>=1;if(kind==='schedule')return n>=2;if(kind==='repeat')return n>=3;return false}
function setStatus(text,mode=''){const el=q('#publishEngineStatus');if(!el)return;el.textContent=text;el.className='publish-status '+mode}
function msg(id,text,mode=''){const el=q(id);if(!el)return;el.textContent=text;el.className='publish-message '+mode}
function renderSubject(){const s=subject(),c=context()||{};q('#publishSubjectType').textContent=s.type.toUpperCase();q('#publishSubjectLabel').textContent=s.type==='person'?'나의 개인 브랜드':(c.workspaceName||c.storeName||c.tenant||'현재 Workspace');q('#publishPlanGate').textContent=plan().toUpperCase()+' · '+(allowed('schedule')?'예약 가능':allowed('now')?'즉시 발행':'제작 중심');}
async function loadBrand(){try{const d=await api('/v1/brand');if(d.brand){q('#brandName').value=d.brand.brand_name||'';q('#brandTagline').value=d.brand.tagline||'';q('#brandAudience').value=d.brand.audience_summary||''}}catch(e){msg('#brandMessage',e.message,'error')}}
function renderChannels(list=[]){const host=q('#publishChannelList');host.replaceChildren();selected.clear();if(!list.length){host.innerHTML='<p class="publish-empty">아직 연결된 발행 채널이 없습니다. 채널 인증 연결 후 자동 게시가 열립니다.</p>';syncButtons();return}for(const ch of list){const row=document.createElement('label');row.className='publish-channel '+(ch.status==='active'?'':'disabled');const box=document.createElement('input');box.type='checkbox';box.value=ch.id;box.disabled=ch.status!=='active';box.addEventListener('change',()=>{box.checked?selected.add(Number(ch.id)):selected.delete(Number(ch.id));syncButtons()});const name=document.createElement('strong');name.textContent=ch.display_name;const state=document.createElement('span');state.textContent=(ch.provider||'')+' · '+ch.status;row.append(box,name,state);host.append(row)}syncButtons()}
async function loadChannels(){try{const d=await api('/v1/channels');renderChannels(d.channels||[]);setStatus('발행 엔진 연결됨','ready')}catch(e){renderChannels([]);setStatus('연결 확인 필요','warn');msg('#publishMessage',e.message,'error')}}
function formatTime(v){if(!v)return'-';const d=new Date(v);return Number.isNaN(d.getTime())?v:d.toLocaleString('ko-KR',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'})}
function renderJobs(list=[]){const host=q('#publishJobList');host.replaceChildren();if(!list.length){host.innerHTML='<p class="publish-empty">아직 발행 작업이 없습니다.</p>';return}for(const job of list.slice(0,30)){const row=document.createElement('div');row.className='publish-job';const title=document.createElement('div');const strong=document.createElement('strong');strong.textContent=job.title||job.caption?.slice(0,34)||'콘텐츠';const channel=document.createElement('span');channel.textContent=(job.display_name||job.channel_type||job.provider)+' · '+formatTime(job.scheduled_at);title.append(strong,channel);const kind=document.createElement('span');kind.textContent=job.schedule_kind+(job.recurrence_rule?' · '+job.recurrence_rule:'');const state=document.createElement('b');state.className='job-'+job.status;state.textContent=job.status;row.append(title,kind,state);host.append(row)}}
async function loadJobs(){try{const d=await api('/v1/jobs');renderJobs(d.jobs||[])}catch(e){msg('#publishMessage',e.message,'error')}}
function currentContent(){const caption=String(q('#trialCaption')?.textContent||'').trim();const tags=String(q('#trialTags')?.textContent||'').trim();if(!caption)return null;const subjectName=String(q('#trialSubject')?.value||q('#brandName')?.value||'').trim();return{title:subjectName?subjectName+' 홍보 콘텐츠':'Marketing AI 콘텐츠',contentType:'social_post',caption:[caption,tags].filter(Boolean).join('\n\n'),source:'ai',data:{generatedBy:'ekodi-marketing-ai'}}}
function localIso(value){if(!value)return'';const ms=Date.parse(value);return Number.isNaN(ms)?'':new Date(ms).toISOString()}
function syncButtons(){const has=selected.size>0;q('#publishNow').disabled=!has||!allowed('now');q('#publishSchedule').disabled=!has||!allowed('schedule')}
async function queue(immediate){const content=currentContent();if(!content)return msg('#publishMessage','먼저 콘텐츠 실습실에서 게시할 문구를 만들어 주세요.','error');if(!selected.size)return msg('#publishMessage','발행할 활성 채널을 하나 이상 선택해 주세요.','error');const recurrence=String(q('#publishRecurrence').value||'');if(recurrence&&!allowed('repeat'))return msg('#publishMessage','반복 발행은 PRO 이상에서 사용할 수 있습니다.','error');let scheduledAt='';if(!immediate){scheduledAt=localIso(q('#publishAt').value);if(!scheduledAt)return msg('#publishMessage','예약 날짜와 시간을 선택해 주세요.','error')}const button=immediate?q('#publishNow'):q('#publishSchedule');button.disabled=true;const old=button.textContent;button.textContent='등록 중…';try{const d=await api('/v1/publish',{method:'POST',body:JSON.stringify({content,channelIds:[...selected],scheduledAt:immediate?new Date().toISOString():scheduledAt,recurrenceRule:recurrence,requestedBy:'human'})});msg('#publishMessage',d.scheduleKind==='immediate'?'발행 큐에 등록했습니다. 실제 게시 결과까지 추적합니다.':'예약했습니다. 지정 시간에 자동 발행하고 결과를 확인합니다.','ok');await loadJobs()}catch(e){msg('#publishMessage',e.message,'error')}finally{button.textContent=old;syncButtons()}}
q('#brandProfileForm')?.addEventListener('submit',async e=>{e.preventDefault();const brandName=String(q('#brandName').value||'').trim();if(!brandName)return msg('#brandMessage','브랜드 이름을 입력해 주세요.','error');const b=e.submitter;b.disabled=true;try{await api('/v1/brand',{method:'PUT',body:JSON.stringify({brandName,tagline:q('#brandTagline').value,audienceSummary:q('#brandAudience').value})});msg('#brandMessage','개인/Workspace 브랜드 프로필을 저장했습니다.','ok')}catch(err){msg('#brandMessage',err.message,'error')}finally{b.disabled=false}});
q('#refreshPublishChannels')?.addEventListener('click',loadChannels);q('#refreshPublishJobs')?.addEventListener('click',loadJobs);q('#publishNow')?.addEventListener('click',()=>queue(true));q('#publishSchedule')?.addEventListener('click',()=>queue(false));q('#publishRecurrence')?.addEventListener('change',syncButtons);
async function boot(){renderSubject();if(!token()){setStatus('Google 로그인 필요','warn');syncButtons();return}setStatus('발행 엔진 확인 중');await Promise.all([loadBrand(),loadChannels(),loadJobs()]);syncButtons()}
window.addEventListener('ekodi:workspace-ready',boot);window.addEventListener('ekodi:auth-ready',boot);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,0),{once:true});else setTimeout(boot,0);
})();</script>`;
html=html.replace('</body>',`${runtime}</body>`);
html=html.replace(/connect-src([^;]*);/i,(all,hosts)=>hosts.includes(API)?all:`connect-src${hosts} ${API};`);

fs.writeFileSync(hub,html);

const check=fs.readFileSync(hub,'utf8');
for(const marker of ['class="publication-ops"','id="publishNow"','id="publishSchedule"','marketing-publish-api.ekodi.kr','window.EKODI_MARKETING_AUTH_TOKEN','mode\')===\'personal-brand\'','/v1/publish','credentials_required']){
  if(!check.includes(marker))throw new Error(`Publishing workspace marker missing: ${marker}`);
}
console.log('Marketing publishing workspace patched into hub.');
